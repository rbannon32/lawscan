# BigQuery Configuration for API
PROJECT_ID=lawscan
DATASET=ecfr
TABLE=sections

# Alternative: Point to enhanced dataset with custom metrics
# DATASET=ecfr_enhanced
# TABLE=sections_enhanced

# Alternative: Point to historical backfill data
# DATASET=ecfr_historical
# TABLE=sections

# Optional: Path to service account key (if not using default auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
